Drinks page: group by occasion + tile styling

src/app/account/drinks/page.tsx

Replace the flat 50-tile grid with 8 sections grouped by primary
occasion (first item in each drink's occasions array). Sections
follow the same OCCASIONS order as the taxonomy: Everyday hydration,
Post-workout, Afternoon pick-me-up, Dinner mocktail, Party / celebration,
Cosy evening, Dessert replacement, Batch & top up.

Each section gets a dk-occ-header with a 1.5px ink underline to match
the quit-list region headers, so all three companion pages read as a
family. Tile number reduced from 64px to 52px so the coral stops
dominating when 50 of them are on screen.

Filter behaviour preserved: filter by flavour, by occasion, or both
(intersection). No filters shows all 50 drinks across 8 sections.

scripts/commit-msg.txt: deleted (stale file left from prior session).