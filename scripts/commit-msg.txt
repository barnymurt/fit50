On the house refactor + new sub-pages + PDF downloads for drinks

Sign up section (SixFeatures):
- Removed the entire 'The math' block: the EUR 6 / 50 days / EUR 300 you keep stat tiles + the long 'A caneca is ~EUR 6...' headline. The user wanted this gone.

On the house (was Resources):
- Section renamed from 'Resources' to 'On the house' (eyebrow + Hero).
- Removed the 'Sleep + steps' card and the 'Workout inspiration' card. Left the four that map to the actual tools/users: Smoking cessation / Drinking less / Meditation / Nutrition (macro calculator).
- Each card now points to a real page (the four HTML files the user provided).
- The Resources section on the homepage still ships with the old aria-label 'Resources' so screen readers don't churn.

New sub-pages under /account:
- /account/quit-list: 18 of the 40 services from quit-list.html, structured by region (Europe / North America / South America / Africa / Asia / Oceania), each card links to the official website/phone. Modal shows full details + notes. Same lazy-modal pattern as FoodDetail.
- /account/drinks: 18 of the 50 drinks from drinks.html, each as a card with kcal + macros + flavours. Modal shows ingredients + method + macros + 'batch trick' notes. Has a 'Download as PDF card' button that opens a print-styled window (window.open + window.print) so the user can save the drink as a one-recipe PDF. Each drink is its own PDF.
- /account/meditation: All 8 apps from ten-minutes.html, structured by Free / Premium. Modal shows full price + platforms + best-for + strengths + link out.

Data for the three pages lives in src/data/on-the-house.ts (typed Drink / QuitService / MeditationApp records with a constant per-page). Modal extracted to src/components/Modal.tsx so the three pages share the same bottom-sheet-on-mobile / dialog-on-desktop pattern as FoodDetail.

/account for non-premium users:
- The 'Free tier' block (which is what non-premium users see instead of the food / timer / kanban sections) now calls out the specific tools they unlock: 'Unlock the detailed macro food tracker, streak protection, water tracker, multi-purpose timer, kanban board, and to-do list. One payment, yours forever.'

Build clean. /account/quit-list 1.5 kB. /account/drinks and /account/meditation also statically prerendered.