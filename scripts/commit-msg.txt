Quit List: remove Antarctica coda-note, change coda line, white tiles with no text clipping

src/app/account/quit-list/page.tsx:

  - Coda text now reads 'Fifty days smoke-free starts tomorrow.' (was '...starts with one call.').
  - Removed the Antarctica coda-note block entirely (the user asked to delete that whole blockquote/note).
  - The coda section is now just the headline + coda-note gone; the section still has the coda padding and spacing so the layout doesn't collapse.

  - .ql-tile background changed from var(--ql-paper) to #ffffff (pure white, as the user asked).
  - The rounded corners stay (border-radius: var(--ql-radius) which is 20px - the same as before).
  - To make sure no text gets clipped at the rounded corners, added overflow-wrap: anywhere on the .ql-tile and on the three text-heavy children (.ql-tile-name, .ql-tile-org, .ql-tile-blurb) so any long service/org/blurb text breaks inside the tile rather than overflowing the corner.
  - .ql-tile-name font-size dropped from 20px to 18px and line-height from 1.1 to 1.2 to give the long names more vertical room.
  - .ql-tile-blurb font-size dropped from 13.5px to 12.5px so the blurb still has room to grow inside the tile without being clipped by the rounded bottom corners.

Build clean.