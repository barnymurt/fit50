Update Sign up section copy + switch checkout env to Stripe

User requested changes to the Sign up section:

1. Marquee text: 'BUY US A CANECA - KEEP THE TOOLS FOREVER' (was the previous rolling text)
2. Subhead: 'Walk away with six tools you could save yourself EUR 300.' (replaces 'and we'll give you the tools to not need one for 50 days.')
3. Removed the math block entirely (the EUR 6 caneca / 50 days / EUR 300 you keep stat tiles + the 'A caneca is ~EUR 6...' headline). The new subhead now states the EUR 300 savings in a single line.
4. 'What you get' section heading changed from 'Sign up for helpful tools.' to 'Six tools to walk into the challenge.'
5. Removed the 'Pay with the same email you'll use to sign in...' hint from the CTA. The premium state still surfaces a clear 'You're in' panel.
6. Removed the 'Free for everyone' /toolkit callout block entirely.
7. Switched checkout provider from Creem to Stripe:
   - Renamed env var NEXT_PUBLIC_CREEM_CHECKOUT_URL -> NEXT_PUBLIC_STRIPE_CHECKOUT_URL in both the homepage SixFeatures and /macrocalc page
   - When the env var is missing, the error message now says 'Stripe checkout is not configured yet. We are switching over from Creem - check back soon.' so users know the swap is in progress
   - The actual Stripe integration (Stripe Checkout session creation, webhook handler, RLS update on profiles table) is left for a follow-up once Stripe keys are wired up. The placeholder URL still uses prefilled_email which maps to Stripe's customer_email param.
   - CTA caption updated: 'secure checkout via Stripe' (was 'via Creem')

The Sign up section is still a single <section> on the homepage. Internal flow: marquee -> hero (eyebrow/headline/subhead/one-liner) -> What you get (heading + 6 feature grid) -> CTA.

No new components added; no public-API changes.