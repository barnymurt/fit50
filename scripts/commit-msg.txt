Revert Nav to the pre-layout-fix version (the 'new' one)

The user wants the original Nav bar only, so the layout-fix commit (50f09f4, which added gap-4, shrink-0, and whitespace-nowrap to prevent desktop nav overlap) is reverted. The other Nav renames from earlier commits (Resources -> On the house, CTA -> Buy us a beer, order changed so FAQ comes before On the house) are kept.

Reverted to commit 0812c52. Build clean.