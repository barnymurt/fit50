Modal content now centered

The modal dialog itself was already centered on screen (it's a <dialog> element). The CONTENT inside was left-aligned. Updated the CSS in src/styles/on-the-house.css:

- .modal-body: text-align: center, sets the default for every child.
- .modal-blurb: text-align: center (inherits), max-width 520px and margin auto so the lede reads as a centred paragraph block.
- .modal-section-title: stays centred (inherits body text-align).
- .modal-support, .modal-feats: justify-content: center on the flex row so the chip groups sit centred.
- .modal-macros: max-width 520px with margin auto so the 4-cell macros card (drinks modal) is centred.
- .modal-meta, .modal-ingredients, .modal-method, .modal-note: explicitly set to text-align: left (override the centred body) so the detail blocks read naturally, each constrained to 520px and centred as a block.
- .modal-actions: justify-content: center so the row of buttons (Visit site, Copy details, Print) sits centred.

Visual character preserved: the italic Fraunces coral eyebrow ('No. 03 - Europe'), the Fraunces 600 modal-title, the coral-vibrant support chips, the lavender-soft note card, the modal-close button, the shadow. Now everything reads as a centered card with detail blocks nested inside.

Build clean.