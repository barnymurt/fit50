Resources section moved below FAQ; nav order updated to match

- src/app/page.tsx: <Resources id="resources" /> now rendered after <FAQ />, so the On the house section is the last content block on the homepage (Hero, Rules, Calculator, Workouts, Tracker, Sign up, FAQ, On the house).
- src/components/Nav.tsx: NAV_LINKS reordered to match the new homepage flow (Rules, Workouts, Tracker, FAQ, On the house). The mobile menu reads from the same array so it auto-updates.

Build clean.