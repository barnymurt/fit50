Swap tones: quit list -> teal, meditation -> cream

The three sub-pages now carry three distinct tones again:
- drinks:   lavender
- meditation: cream  (was teal)
- quit-list:  teal   (was cream)

Swap was triggered because cream felt wrong for the quit-list tone.

src/app/account/meditation/page.tsx
  .tm-root base background: var(--teal) -> var(--color-cream),
  color override at line 369 (var(--tm-ink)) now stands alone, which
  is the right colour for ink text on cream.
  .tm-marquee and .tm-filter-wrap: var(--teal)/var(--paper) ->
  var(--color-cream)/var(--tm-ink). All other --tm-* tokens and
  pills/tiles/empty/modal backgrounds stay paper.

src/app/account/quit-list/page.tsx
  Token block: --ql-cream (#F2D9A2) replaced by --ql-teal (#4A9B9B).
  .ql-root base: --ql-cream/--ql-ink -> --ql-teal/--ql-paper (teal
  carries paper text per design system).
  .ql-marquee and .ql-filter-wrap: --ql-cream -> --ql-teal, with
  color: var(--ql-paper) added so text on the sticky surfaces stays
  legible. Lavender tokens remain defined but unused at the page
  level (modal notes still use --ql-lavender-soft for soft callouts).

Build clean. /account/quit-list and /account/meditation unchanged size.